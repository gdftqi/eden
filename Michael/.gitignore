# Unity 自动生成, 每次打开工程都会重建 -- 根 .gitignore 已挡住
# Library/ Temp/ Logs/ UserSettings/ *.slnx, 这里只补它没覆盖的.

# Unity 按 Packages/manifest.json 重新生成的工程文件.
# 不能写成全局 *.csproj -- 仓库里 Lilith 和 CC 是手写工程, 必须入库
*.csproj

# Unity 探测到 VS 时写的组件清单
.vsconfig

# 烘焙/导入产物
/[Aa]ssets/[Ss]treamingAssets/aa/
/[Aa]ssets/AssetStoreTools*
crashlytics-build.properties

# 包管理器本地缓存
.gradle/
